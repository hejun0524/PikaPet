The next fix is as follows:

1. Auto-update for the main app

Goal: implement update-and-restart the way VSCode/Discord/GitHub Desktop do it — background check, silent download+install, then a "Restart to Update" UI element the user clicks on their own schedule (not a forced immediate relaunch).

1. Add `@tauri-apps/plugin-updater` and `@tauri-apps/plugin-process` (and the Rust `tauri-plugin-updater` crate), wire up the Rust plugin registration in `lib.rs`, and add the required permissions in the capabilities config.
2. Implement a background check (on app startup, and on an interval — suggest something reasonable like every few hours, make it configurable) using `check()`.
3. If an update is found, download it in the background with `update.download()`, then call `update.install()` once downloaded — do NOT call `relaunch()` automatically.
4. Expose app state / an event so the frontend can show a persistent "Restart to Update" button once install() has completed. Clicking it calls `relaunch()`.
5. Handle the Windows caveat explicitly: Windows exits the app automatically when `install()` runs, so on Windows, defer calling `install()` until the user clicks the restart button (download in the background as normal, but combine download+install+implicit relaunch behind the click). Document this platform difference clearly in code comments.
6. tauri.conf.json: set `createUpdaterArtifacts: true`, and set `endpoints` to the standard GitHub Releases `latest.json` pattern (e.g. `https://github.com/<owner>/<repo>/releases/latest/download/latest.json`), with `<owner>/<repo>` left as a placeholder for me to fill in. Stub the `pubkey` field too — don't invent a value, leave a clear placeholder and tell me exactly what I need to generate to fill it in.

General instructions:
- Use Tauri v2 APIs throughout (not v1).
- Show me the resulting file/directory structure changes for the add-ons rework before making sweeping changes.
- Flag any place where you're making an assumption about my existing codebase structure — ask instead of guessing if you can't find the current add-on loading code.
- Add basic tests where reasonable (manifest validation, permission parsing).